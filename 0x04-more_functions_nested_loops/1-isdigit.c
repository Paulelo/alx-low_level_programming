#include <"main.h>

/**
* _isdigit- check for number from 0 through 9
* @c: is the integr used with the function _isdifit
* Return: 0
*/
int _isdigit(int c)
{
	if (c = 0 && c <= 9)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	_putchar('\n');
}
